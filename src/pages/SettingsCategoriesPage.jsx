function SettingsCategoriesPage({ categories, onBack, onManageCategories }) { return <div className="screen settings-screen settings-detail-screen"><header className="topbar"><button className="back-btn" aria-label="Вернуться к настройкам" type="button" onClick={onBack}>← Назад</button><div><p className="eyebrow">Настройки</p><h2>Категории</h2></div></header><section className="card settings-section"><h4>Доходы, расходы и лимиты</h4><p className="muted">Создавайте, редактируйте и удаляйте категории. Лимиты расходов настраиваются там же.</p><div className="settings-category-preview">{categories.filter((category) => !category.archived).slice(0, 5).map((category) => <span key={category.id}>{category.icon} {category.name}</span>)}</div><button className="primary-btn settings-wide-btn" type="button" onClick={onManageCategories}>Управление категориями</button></section></div>; }
export default SettingsCategoriesPage;
