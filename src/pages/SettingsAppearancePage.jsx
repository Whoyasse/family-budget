import { ACCENTS, THEMES } from '../utils/settingsStorage';
function SettingsAppearancePage({ settings, onSettingsChange, onBack }) { return <div className="screen settings-screen settings-detail-screen"><header className="topbar"><button className="back-btn" aria-label="Вернуться к настройкам" type="button" onClick={onBack}>← Назад</button><div><p className="eyebrow">Настройки</p><h2>Оформление</h2></div></header><section className="card settings-section"><h4>Акцент</h4><div className="accent-grid">{Object.entries(ACCENTS).map(([id, accent]) => <button key={id} type="button" className={settings.accent === id ? 'active' : ''} onClick={() => onSettingsChange({ ...settings, accent: id })}><i style={{ background: accent.primary }} />{accent.label}</button>)}<label className={`custom-accent-picker ${settings.accent === 'custom' ? 'active' : ''}`}><i className="color-wheel"/><span>Свой цвет</span><input aria-label="Выбрать свой акцент" type="color" value={settings.customAccent || '#2fcf72'} onChange={(event) => onSettingsChange({ ...settings, accent: 'custom', customAccent: event.target.value })}/></label></div><div className="accent-preview"><span>Предпросмотр</span><button className="primary-btn" type="button">Основная кнопка</button></div></section><section className="card settings-section"><h4>Тема</h4><div className="choice-grid theme-grid">{Object.entries(THEMES).map(([id, theme]) => <button key={id} type="button" className={settings.theme === id ? 'active' : ''} onClick={() => onSettingsChange({ ...settings, theme: id })}>{id === 'dark' ? '🌙' : '☀️'} {theme.label}</button>)}</div></section></div>; }
export default SettingsAppearancePage;
